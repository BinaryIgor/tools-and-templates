{
  "volumes": [
    "${secrets-path}:/secrets:ro"
  ],
  "build_cmd": "bash build_cmd.bash",
  "env": {
    "JVM_OPTS": "-Xmx450m",
    "DB_URL": "${db-url}",
    "DB_USER": "${db-user}",
    "DB_PASSWORD": "secrets:db-password",
    "JWT_TOKEN_KEY": "secrets:jwt-token-key"
  },
  "secrets": [
    "db-password",
    "jwt-token-key"
  ],
  "network": "host",
  "memory": "500M",
  "cpus": "0.5"
}