{
  "volumes": [
    "${SECRETS_DIR}:/secrets:ro"
  ],
  "build_cmd": "bash build_cmd.bash",
  "env": {
    "JVM_OPTS": "-Xmx450m"
  },
  "secrets": {},
  "network": "host",
  "memory": "500M",
  "cpus": "0.5"
}