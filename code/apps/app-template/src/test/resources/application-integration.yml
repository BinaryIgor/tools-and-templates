spring:
  datasource:
    #    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

app:
  email:
    fake-server: true
  jwt:
    token-key: "and0LWxvY2FsLXRva2VuCg=="