spring:
  datasource:
    #    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

app:
  email:
    fake-server: true
  jwt:
    #valid, random token key for testing tokens
    token-key: "xCwZ4zydRbu8kbKre59Hi2224Xdz0PupADTWkErvIg4"